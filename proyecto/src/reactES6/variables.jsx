<>
    <p>hay 3 fromas de declarar una variable.
        let
        var
        const

        Las variables var tienen alcance global

        Las variables let y const tienen alcance de bloques.
        si declara una variable fuera de un bloque, tiene alcance global, 
        pero si la declaras dentro de un for por ejemplo, solo prodras usarla 
        dentro del for.

        las variables const son una referencia constante a un valor.
        por eso no puedes:
            reasignar un valor
            reasignar un array
            reasignar un objeto
        pero puedes:
            cambiar los elementos de un array
            cambiar las propiedades de un objeto
        
        usando useEstate tambien se pueden cambiar los valores de constantes.
    </p>
</>